services:
  api:
    build: .
    container_name: safepay
    ports:
      - "8080:8080"
    environment:
      DB_PATH: /data/safepay.db
    volumes:
      - safepay_data:/data
    restart: unless-stopped

volumes:
  safepay_data:
